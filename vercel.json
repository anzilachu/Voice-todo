{
  "buildCommand": "prisma generate && next build",
  "outputDirectory": ".next",
  "rewrites": [
    { "source": "/manifest.json", "destination": "/api/manifest" }
  ],
  "env": {
    "NEXTAUTH_URL": "voice-todo-rfa7.vercel.app"
  }
}